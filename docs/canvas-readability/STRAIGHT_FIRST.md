# Straight connections first — 2026-09-06

Canvas layout v6 extends facing-port alignment to one-way relationships. A busier endpoint and a less connected endpoint no longer retain unrelated port offsets when a common clear axis is available. Candidate ports stay within both node borders, avoid occupied ports, and must have an unobstructed segment between them. Card and header clearance remains enforced.

For aligned unobstructed endpoints, crossing and shared-segment penalties are disabled so the shortest straight connection wins. Crossings remain visually separated by the renderer. The backend still owns every coordinate; the browser does not straighten or reroute paths. Reciprocal routing and all directed IDs and facts are retained. Overview now completes an eligible reciprocal pair using the return relationship already supplied by the server, even when only one member is in the backbone selection. It draws one line with two arrowheads and counts both relationships. Authored journeys and Trace Path retain exact directional selection; missing server relationships are never fabricated. The Inventory service / Business event bus pair is covered by this rule.

Compared with the saved v5 references, straight directed routes increase from 13 to 18 in Commerce, 6 to 15 in AI Workspace, and 10 to 18 in Manufacturing. All 29 previously straight routes remain straight; 22 avoidable doglegs become two-point straight segments. Accepted Architecture data, node boxes, and group frames remain unchanged. Not every connection can be straight: actual intervening cards, disjoint facing spans, and occupied ports still constrain routing.

Validation: 47 focused Python tests and 5 Node checks pass. New regressions cover unequal port loads in both orientations, real intervening obstacles, order determinism, and crossing another relationship without bending a clear straight line. The actual renderer passes all three references, nine journeys, reciprocal selection, 27 group collapse/expand checks, and arrow alignment at 100%, 200%, and 400%, with zero page/console errors.

Evidence and interactive reader: `C:/AI/temp/archbro/2609051136/results/final-consolidated-20260905T0932/canvas-straight-20260906/`. See `straight-route-comparison.json`, `geometry.json`, `focused-tests.xml`, and `views/visual-results.json`. Offline projection verification only; no release acceptance or runtime deployment is claimed.
